# AgroSys Mobile

Aplicativo mobile do sistema **AgroSys**, desenvolvido em **React Native + Expo**.  
Este projeto é responsável pelo **front-end mobile**, consumindo a API web já existente do sistema.

---

## 🚀 Pré-requisitos

- Node.js 18+
- npm ou yarn
- Expo CLI (`npm install -g expo-cli`)

---

## 📂 Estrutura do Projeto

```
agrosys-mobile/
├── app/                                   # Rotas da aplicação (Expo Router)
│   ├── (auth)/                             # Agrupa telas relacionadas à autenticação
│   │   ├── login.tsx                       # Tela de Login
│   │   └── register.tsx                    # Tela de Cadastro
│   │
│   ├── (tabs)/                             # Navegação principal com abas
│   │   ├── home.tsx                        # Tela inicial acessada pelo usuário
│   │   ├── profile.tsx                     # Tela de perfil do usuário
│   │   ├── settings.tsx                    # Tela de configurações
│   │   └── _layout.tsx                     # Define o layout das abas (Bottom Tabs)
│   │
│   ├── _layout.tsx                         # Layout raiz das rotas (tema, providers)
│   └── index.tsx                           # Tela inicial / Splash / Redirecionamento
│
├── src/                                    # Código-fonte organizado por domínio
│   ├── api/                                # Comunicação com a API (backend)
│   │   ├── client.ts                       # Configuração do Axios (baseURL, interceptors)
│   │   └── auth.ts                         # Funções de login, logout, refresh token etc.
│   │
│   ├── components/                         # Componentes reutilizáveis do projeto
│   │   └── ui/                             # Componentes de UI da pasta antiga (mover para cá)
│   │
│   ├── screens/                            # Telas completas separadas, usadas pelas rotas
│   │   ├── LoginScreen.tsx                 # Comportamento da tela de login
│   │   └── HomeScreen.tsx                  # Comportamento da tela inicial
│   │
│   ├── stores/                             # Gerenciamento de estado
│   │   └── useAuth.ts                      # Estado global de autenticação (Zustand)
│   │
│   ├── theme/                              # Configurações de tema e estilos
│   │   ├── colors.ts                       # Paleta de cores da aplicação
│   │   ├── fonts.ts                        # Tipografias utilizadas
│   │   └── index.ts                        # Export central do tema
│   │
│   ├── core/                               # Infraestrutura do app
│   │   ├── config/                          # Configurações gerais (ex: env, constantes)
│   │   ├── http/                            # Lógicas de requisição (tratamento de erro)
│   │   ├── storage/                         # Persistência (AsyncStorage)
│   │   └── utils/                           # Funções utilitárias (formatadores, validadores)
│   │
│   └── services/                            # Regras de negócio (ex: serviço de vendas, estoque)
│
├── assets/                                 # Imagens, ícones, fontes
├── .vscode/                                # Configurações do editor VSCode
├── app.json                                # Configurações do Expo
└── package.json                            # Dependências e scripts do projeto


```

---

## 📦 Scripts Disponíveis

- `npm start` → inicia o Expo (modo desenvolvimento)
- `npm run android` → abre no Android
- `npm run ios` → abre no iOS (Mac)
- `npm run web` → abre no navegador

---

## 🛠️ Boas Práticas

- **TypeScript** para tipagem estática
- **Zustand** para gerenciamento de estado
- **Axios** para comunicação com a API
- **Expo Router** para navegação
- **Estrutura modular** para escalabilidade e organização
- **Componentes reutilizáveis** e centralizados
- **Tema global** para cores e fontes consistentes
- Mensagens de commit claras: `feat`, `fix`, `chore`, `refactor`, `docs`

---

## 🔧 Fluxo de Trabalho

1. Atualizar o projeto:

```bash
git pull origin main
```

2. Criar uma branch para sua feature:

```bash
git checkout -b feat/nome-da-feature
```

3. Adicionar alterações:

```bash
git add .
```

4. Commitar com mensagem clara:

```bash
git commit -m "feat: adicionar login screen"
```

5. Subir para o GitHub:

```bash
git push origin feat/nome-da-feature
```

6. Abrir Pull Request para revisão no GitHub.

> Sempre use branches (nunca commit direto na main) e commits pequenos e claros.

---

## 🏗️ Estrutura das Telas e Navegação

- **(auth)** → Telas públicas:

  - `login.tsx`
  - `register.tsx`

- **(tabs)** → Telas internas do app:

  - `home.tsx`
  - `profile.tsx`
  - `settings.tsx`

- `_layout.tsx` → layout raiz, define providers e estilo global

---

## 📖 Contribuição

- Faça um fork do projeto
- Crie uma branch para sua feature
- Commit com mensagens claras e pequenas
- Suba a branch e abra Pull Request para revisão
